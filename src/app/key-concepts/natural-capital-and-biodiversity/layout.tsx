import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Natural Capital and Biodiversity",
};

export default function BiodiversityLayout({ children }: { children: React.ReactNode }) {
  return children;
}
